# CodeAlpha_docker-webserver
A Dockerized web server project that uses Nginx to host a static HTML webpage. This project demonstrates Docker image creation, container deployment, port mapping, and serving web content through a Docker container.
